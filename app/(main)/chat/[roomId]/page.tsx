const ChatByIdPage = ({ params }: { params: { roomId: string } }) => {
  const { roomId } = params;

  return <div className="w-full h-full">This is chat by id page</div>;
};

export default ChatByIdPage;
